<nav class="navbar navbar-expand-md">
  <a class="navbar-brand" href="/#">Podcasher</a>
  <ul class="navbar-nav ml-auto">
  <% if user_signed_in? %>
    <li class="nav-item d-none d-md-inline-block">
      <%= link_to t('user.sign_out'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-primary' %>
    </li>
  <% end %>
  
  </ul>
</nav>